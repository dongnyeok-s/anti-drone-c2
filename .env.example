# 대드론 C2 시뮬레이터 환경 변수 설정
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값을 입력하세요

# ============================================
# 시뮬레이터 서버 설정
# ============================================
SIMULATOR_PORT=8080
SIMULATOR_WS_URL=ws://localhost:8080

# ============================================
# 프론트엔드 설정
# ============================================
VITE_SIMULATOR_WS_URL=ws://localhost:8080
VITE_FRONTEND_PORT=3000

# ============================================
# 음향 모델 설정
# ============================================
AUDIO_MODEL_WS_URL=ws://localhost:8080
AUDIO_DETECTION_INTERVAL=2.0

# ============================================
# 로깅 설정
# ============================================
LOGS_DIR=./logs
LOG_CONSOLE_OUTPUT=false
LOG_ENABLED=true

# ============================================
# 시나리오 설정
# ============================================
SCENARIOS_DIR=./scenarios/generated

# ============================================
# 개발 모드 설정
# ============================================
NODE_ENV=development

# ============================================
# 보안 설정
# ============================================
# 인증 활성화 (프로덕션에서 권장)
AUTH_ENABLED=false
# 인증 토큰 (AUTH_ENABLED=true일 때 필수)
# AUTH_TOKEN=your-secret-token-here

# CORS 설정
CORS_ENABLED=true
CORS_ORIGIN=*

# Rate Limiting (DoS 공격 방지)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000
